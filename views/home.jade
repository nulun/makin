extends layout


block content
	#body-box.max-box
		.newpost-box.my-hidden
			form.ui.form(role='form', action="/postcode",method="post")
				.field
					label your code
					textarea(name = 'code', rows = '12')
				.field
					label your code language
					input(type = 'text', name='lang')
				.field
					label say something?
					input(type = 'text', name='title')
				button.ui.button(type = 'submit') ok, that's it
		.newpost-mask.my-hidden.pointer
		.slide-box-1.max-box
			.menu-box
				.ui.secondary.pointing.menu.h-tab
					.newcode
					//textarea.newpost(placeholder='', spellcheck="false")
					.logo.pointer(onclick="location.href='/'")
						p instacode.
					//a.item.active Trending
					//a.item Followed
					//a.item.click-2 撰写新食谱
					.subm-box.my-hidden.postcode
						.submcode
					.like-color.nav-heart
					canvas.h-avatar.pointer#h-avatar(name = '#{user.username}', onclick="location.href='/profile'")
			.small-mask.my-hidden
				textarea.newpost(placeholder='', spellcheck="false")
			.h-container
				.h-box(name='#{code[0]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[0].postedBy}
						.time
							p #{code[0].date}
						.lang
							p
					.code
						pre
							code #{code[0].code}
					.foot
						if (code[0].liked)
							.heart.icon.liked
						if (!code[0].liked)
							.empty.heart.icon
						p #{code.likedBy[0]}
				.h-box(name='#{code[1]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[1].postedBy}
						.time
							p #{code[1].date}
						.lang
							p
					.code
						pre
							code #{code[1].code}
					.foot
						if (code[1].liked)
							.heart.icon.liked
						if (!code[1].liked)
							.empty.heart.icon
						p #{code.likedBy[1]}
				.h-box(name='#{code[2]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[2].postedBy}
						.time
							p #{code[2].date}
						.lang
							p
					.code
						pre
							code #{code[2].code}
					.foot
						if (code[2].liked)
							.heart.icon.liked
						if (!code[2].liked)
							.empty.heart.icon
						p #{code.likedBy[2]}
				.h-box(name='#{code[3]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[3].postedBy}
						.time
							p #{code[3].date}
						.lang
							p
					.code
						pre
							code #{code[3].code}
					.foot
						if (code[3].liked)
							.heart.icon.liked
						if (!code[3].liked)
							.empty.heart.icon
						p #{code.likedBy[3]}
				.h-box(name='#{code[4]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[4].postedBy}
						.time
							p #{code[4].date}
						.lang
							p
					.code
						pre
							code #{code[4].code}
					.foot
						if (code[4].liked)
							.heart.icon.liked
						if (!code[4].liked)
							.empty.heart.icon
						p #{code.likedBy[4]}
				.h-box(name='#{code[5]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[5].postedBy}
						.time
							p #{code[5].date}
						.lang
							p
					.code
						pre
							code #{code[5].code}
					.foot
						if (code[5].liked)
							.heart.icon.liked
						if (!code[5].liked)
							.empty.heart.icon
						p #{code.likedBy[5]}
				.h-box(name='#{code[6]._id}')
					.meta
						canvas.avatar.pointer
						.name
							a #{code[6].postedBy}
						.time
							p #{code[6].date}
						.lang
							p
					.code
						pre
							code #{code[6].code}
					.foot
						if (code[6].liked)
							.heart.icon.liked
						if (!code[6].liked)
							.empty.heart.icon
						p #{code.likedBy[6]}
		.slide-box-2.my-hidden.max-box.share-box
			.s-box#v-wrap
				#recipe-image.image-wrapper
					.img-mask
					img#uploaded-image.center
					i.photo.icon
				p {{ message }}
				br
				form.ui.form(role='form', action="/share",method="post", enctype="multipart/form-data", style='max-width: 300px;')
					.field
						input.ui.input(type='text', name="recipe_name", v-model="message", placeholder='料理标题', required)
					.field
						input.ui.input(type='text', name="brief", placeholder='一句话介绍，可不填')
					.field
						input.ui.input#upload-recipe-image(type='file', name="recipe_image", accept="image/png,image/jpg,image/jpeg")
					.field
						input.ui.input(type='text', name="materials", placeholder='食材种类', required)
					.field
						input.ui.input(type='text', name="procedure", placeholder='料理过程', required)
					button.fluid.ui.black.big.button(type='submit') 提交	
append js
	script(src='/javascripts/home.js')
	script(src='/javascripts/ref/highlight.pack.js')
	script hljs.initHighlightingOnLoad()